#include <bits/stdc++.h> 
using namespace std;

stack<int> pushAtBottom(stack<int>& myStack, int x) 
{
//brute-force approach : TC: O(n), SC: O(n) .. using additional space.. need to optimise ...

//     vector<int>v;
//     while(!myStack.empty())
//     {
//         v.push_back(myStack.top());
//         myStack.pop();
//     }
    
//     reverse(v.begin(),v.end());
    
//     myStack.push(x);
    
//     for(int i=0; i<v.size(); i++)
//     {
//         myStack.push(v[i]);
//     }
    
//     return myStack;

//check optimised insert at bottom recursive function in reverse_a_stack.cpp file
}